name: 爬取专业数据

on:
  workflow_dispatch:
    inputs:
      mode:
        description: '爬取模式'
        required: true
        type: choice
        options:
          - 'test'    # 测试模式：1页
          - 'full'    # 完整模式：全部数据
        default: 'test'

permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v3
      
    - name: 设置Python环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
        
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 创建数据目录
      run: mkdir -p data
    
    - name: 爬取专业数据（测试模式）
      if: ${{ github.event.inputs.mode == 'test' }}
      run: |
        python -c "from crawlers.majors import MajorCrawler; MajorCrawler().crawl(max_pages=1)"
    
    - name: 爬取专业数据（完整模式）
      if: ${{ github.event.inputs.mode == 'full' }}
      run: python -m crawlers.majors
      
    - name: 提交并推送更改
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: '📚 更新专业数据 [${{ github.event.inputs.mode }}] #${{ github.run_number }}'
        file_pattern: 'data/majors.json'
        commit_user_name: GitHub Action
        commit_user_email: action@github.com
